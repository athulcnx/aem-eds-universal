export default async function decorate(block) {
  // The footer-links block has 3 rows:
  // Row 1: logo | nav links | social icons
  // Row 2: utility links (privacy, terms, etc.)
  // Row 3: copyright
  // Structure is already correct from plain.html, no DOM restructuring needed.
}
